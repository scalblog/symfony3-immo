{# src/OC/PlateformBudle/Resources/views/Advert/index.html.twig #}

{% extends "OCPlatformBundle::layout.html.twig" %}

{% block title %}
    Accueil - {{ parent() }}
{% endblock %}

{% block ocplatform_body %}

    <h2>Liste des annonces</h2>
    
    <ul>
        {% for advert in listAdverts %}
            <li>
                <a href=" path('oc_platform_view', {'id':advert.id})h }}">{{ advert.title }}</a>
                par {{ advert.author }}, le {{ advert.date|date('d/m/Y') }}
            </li>

        {% else %}
            <li>Pas encore d'annonces publiées</li>
        {% endfor %}
    </ul>

{% endblock %}